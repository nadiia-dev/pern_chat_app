{
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "node --watch --loader=ts-node/esm backend/src/index.ts",
    "build": "tsc && npm install && prisma generate --schema=./backend/prisma/schema.prisma && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "node backend/dist/src/index.js"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "nodemon": "^3.1.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@prisma/client": "^6.9.0",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "prisma": "^6.9.0"
  }
}
